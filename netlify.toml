[build]
  publish = "dist"
  command = "npm run build"

[dev]
  command = "npm run dev:vite"
  port = 3000
  targetPort = 3000
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200